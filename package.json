{
  "name": "permission-ledger",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "validate": "tsx scripts/validate-corpus.ts",
    "test": "tsx lib/compute.test.ts",
    "ledgers": "tsx scripts/print-ledgers.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
